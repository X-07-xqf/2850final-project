ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ com.goodfood.ApplicationKt.module ]
    }
}

database {
    driver = "org.h2.Driver"
    url = "jdbc:h2:file:./data/goodfood;DB_CLOSE_DELAY=-1;MODE=MySQL"
    user = ""
    password = ""
}
